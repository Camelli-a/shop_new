# Styles Guide

项目 UI 风格先按“项目内设计基础层”维护

- `tokens.css`: 全局颜色、阴影等设计变量
- `navigation.css`: 底部导航样式，与 `components/BottomNav.jsx` 配套。
- `home/index.css`: 首页专属样式，包含首页频道、搜索、活动卡片、商品流等规则。
- `App.css`: 全局样式入口，只负责按顺序导入 `styles` 内的样式文件。


